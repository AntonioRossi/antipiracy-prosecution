# AA11393US — Authored Markdown to XML Acceptance Criteria

> **OPERATIVE INTERNAL CONTROL · NOT FOR FILING**
>
> This is the current acceptance contract coupled to
> [`technical description`](technical-description.md).


## Domain pass rule

All six executable outcomes must pass within the shared
[aggregate validation boundary](../../README.md#aggregate-validation-boundary) on the retained
worktree capture. Each registry row names its independent enforcer. A warning, unknown result, inference,
self-report, stored result, detached token, prior run, stale XML, lossy conversion, or undeclared
read cannot satisfy a criterion.

<!-- SSM-MD-AC-TABLE:START -->
| ID | Executable technical outcome | Independent enforcer |
|---|---|---|
| **SSM-MD-AC-01 — Authority and package closure** | Each `authored-markdown-v1` package has one authoritative Markdown and one generated XML. Markdown alone owns adopted content, stable IDs, order, and substantive metadata; XML contains only preserved semantics and classified envelope fields. Package, router, and consumer ownership closes bidirectionally. | structured_source.verify.VerificationContext._control_closure; structured_source.tests.test_registry |
| **SSM-MD-AC-02 — Authored item-surface closure** | Each stable anchor binds one independently recomputed document, claim, or block target. Every semantic endpoint supplies an exact nonempty `(documentId, fragmentId, typed-item digest)` and resolves once. The Pandoc tree, fragment index, typed records and digests, hierarchy/order, authority binding, GFM profile, `authored.xsd`, and converter agree; empty, default, inferred, duplicate, ambiguous, XML-only, unknown, or unsafe states fail. | structured_source.markdown.convert_authored_markdown; structured_source.tests.test_conversion |
| **SSM-MD-AC-03 — Conversion, readable XML, and back-render closure** | Fresh conversion preserves the complete supported ordered Pandoc AST. Candidate XML strict-parses under retained controls and proves exact fragment records and semantic back-render before publication; registered XML equals readable deterministic regeneration, and authored links remain content rather than inferred dependencies. | structured_source.verify.VerificationContext._derive; structured_source.tests.test_conversion |
| **SSM-MD-AC-04 — Determinism and coverage closure** | Coverage independently recomputes every anchor target, binding kind/path/digest, item/field, typed-item digest, back-render node, dependency, hierarchy, order, and generated byte. Converter self-report, storage formatting, and stored evidence have no semantic authority. | structured_source.verify.VerificationContext._derive; structured_source.tests.test_conversion |
| **SSM-MD-AC-05 — Worktree-capture-bound handoff and writes** | Each edge receives only its selected retained-capture representation after full validation. XML and control regeneration prevalidate complete candidates, atomically publish, exact-read replacements, rebuild fresh state, and fully roll back on failure. Reopen, reconversion, repair, inference, fallback, detached state, and prior-state reuse fail. | structured_source.verify.VerificationContext.read_for_consumer; structured_source.tests.test_acceptance |
| **SSM-MD-AC-06 — Field evolution and implementation closure** | Every field, storage law, binding, or typed-digest change coherently updates all affected owners, the contract pair, registries, controls, conversion, back-render, coverage, consumers, tests, generated XML, and handoffs. Retained-capture-wide closure exactly matches all domain/shared code, contracts, schemas, launchers, registered tests, and vectors; missing, extra, alternate, inactive, bypass, compatibility, stored-evidence, or orphaned states fail. | structured_source.verify.VerificationContext._control_closure; structured_source.tests.test_acceptance |
<!-- SSM-MD-AC-TABLE:END -->

## Registry and execution boundary

The domain's data-only registry is the machine-readable source for the six criterion rows. The
marked table is its deterministic projection. The shared validator checks this domain within the
current retained capture and emits only ephemeral technical status. No callback, self-report,
stored result, approval record, detached token, or prior run substitutes for current execution.
