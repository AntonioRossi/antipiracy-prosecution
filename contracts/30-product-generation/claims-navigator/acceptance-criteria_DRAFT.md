# AA11393US — Interactive Claims-to-Specification Navigator: Acceptance Criteria (DRAFT)

> **INTERNAL COUNSEL-REVIEW MATERIAL — NOT FOR FILING.**

This document is the succinct acceptance half of the navigator documentation pair. The
[`technical description`](technical-description_DRAFT.md) defines the
system; this document defines the outcomes that the current implementation must prove. The
machine-readable counterpart is
[`navigator/schema/acceptance.json`](../../../navigator/schema/acceptance.json).

All twenty criteria are mandatory. An `edition` criterion passes independently for both `na` and
`af`; a `shared` criterion passes once for the common implementation; the `bundle` criterion
passes for the one current delivery bundle. Missing, skipped, unknown, stale, ambiguous, or
partially satisfied criteria fail the global gate. The machine-readable counterpart contains only
the ordered IDs, scopes, and current criterion text; tests and implementation details remain code.

<!-- NAV-AC-TABLE:START -->
| ID | Scope | Required outcome |
|---|---|---|
| **AC-01** | edition | All three structured-source domains pass aggregate validation before construction. Each edition consumes exactly its authored claim set and `pct-as-filed-dossier` through XML while preserving Markdown/PDF authority; authored-relation packages are validation prerequisites, not navigator inputs. |
| **AC-02** | edition | The complete claim XML and PCT typed surface, including document roots and typed-item digests, validate before construction. The immutable product model retains only its declared claim, disclosure, provenance, and asset subset with stable identities, exact hierarchy/order/content, applicable typed metadata/digests, and mechanical DOM locators. |
| **AC-03** | edition | Each edition-owned navigator relation set fixes claim-subject-to-PCT-target direction and uses a closed mapping, phrase, gate, disposition, role, status, note, caution, and typed-item-digest endpoint grammar. Upstream relation references, copied assertions, duplicates, stale/inferred/ambiguous/retargeted endpoints fail; reverse links are derived. |
| **AC-04** | edition | Controlled semantic wording resolves only from current wording XML with exact contexts and typed slots, while ordinary interface copy remains template or code owned. |
| **AC-05** | edition | Each edition receives exactly two dependency-free XML handoffs: its authored claim package and `pct-as-filed-dossier`; no authored-relation edge or handoff exists. The gateway binds exact validation reads without reopening them and reads only navigator controls; undeclared inputs, fallback, and semantic-authority writes fail. |
| **AC-06** | edition | The authored handoff is generated XML bytes with `surface=None`, no assets or dependencies, and same-context retained controls; it is secure-parsed without Markdown reconversion or defaults. The PCT handoff carries the same frozen typed surface and exact assets. One immutable typed model is the renderer's sole semantic API. |
| **AC-07** | edition | The exact current NA and AF claim versions, claim and unit census, groups, dependencies, strategy prefixes, artifact names, and edition isolation rules hold. |
| **AC-08** | edition | Every selectable fragment has exactly mapped or counsel-review-required state; candidate targets, cautions, gates, dispositions, phrase rules, and honest no-candidate behavior are complete and internally consistent. |
| **AC-09** | edition | Every dynamic value is escaped for its declared output context; hostile markup, script, URL, style, event-handler, and raw-template inputs remain inert. |
| **AC-10** | edition | Forward/reverse relation data, target order, cycling and highlight bounds, badges, clearing and focus-return instructions, and the complete printable schedule derive from the same relation set and are present in the artifact. |
| **AC-11** | edition | Static HTML exposes semantic non-nested controls, logical order, scoped keyboard instructions, accessible names and states, a live region, non-color indicators, and reduced-motion rules. |
| **AC-12** | edition | The artifact contains claims, disclosure, exact label, disclaimer, legend, provenance, and the complete schedule plus print rules for repetition, clipping, and overflow safeguards. |
| **AC-13** | edition | The artifact contains the exact two-pane grid, independent-scroll, fixed-scroll-owner, 1280 by 720 minimum, and below-minimum stacking CSS rules. |
| **AC-14** | edition | Claims, disclosure, exact label, disclaimer, legend, provenance, and the complete mapping schedule exist outside scripts in readable document order. |
| **AC-15** | edition | The exact CSP, self-contained local-file design, forbidden-capability checks, no-network, no-cookie, no-storage, no-telemetry, and secure XML parsing requirements pass. |
| **AC-16** | edition | Fresh derivations are byte-identical; the current candidate equals regenerated bytes; release seals those exact bytes and emits the exact detached checksum by atomic generated-only writes. |
| **AC-17** | edition | The exact current product label, standing disclaimer, confidentiality legend, provenance, internal-anchor caution, and strategy-neutral wording are present in ordinary HTML and the print/no-script surfaces. |
| **AC-18** | edition | One structured-source handoff boundary and one navigator-control gateway feed one immutable model. Computed coverage and substantive-origin tracing are complete and non-stored; only current controls and products remain, with Git as sole history. |
| **AC-19** | shared | All three preparatory acceptance contracts and registries pass in one aggregate validation before handoff. The exact twenty-criterion navigator registry, isolated discovered tests, document/source-manifest checks, and unchanged-final-snapshot rule pass. |
| **AC-20** | bundle | The deterministic STORE ZIP contains exactly both sealed HTML files, their exact detached checksums, and MANIFEST.txt; the manifest contains the exact product label, neutral two-edition wording, and checksums for the four non-manifest members, and the ZIP has its own detached checksum. |
<!-- NAV-AC-TABLE:END -->

## Acceptance boundary

Acceptance is one successful, snapshot-bracketed execution of:

```sh
uv --no-cache --offline run --locked --no-sync python -m navigator validate-current
```

The result applies only to the exact unchanged Git checkout that was validated. It proves machine
conformance to the current documentation pair; it does not prove human attention, source-PDF
authenticity or substantive transcription correctness, legal correctness, counsel approval,
filing readiness, filing authorization, or execution by a browser, print engine, or assistive
technology. No contributor self-approval or stored evidence file is required.
