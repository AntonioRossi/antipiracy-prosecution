[project]
name = "aa11393us-prosecution-tools"
version = "0.0.0"
requires-python = "==3.13.12"
dependencies = []

[tool.uv]
package = false
required-version = "==0.11.32"
python-downloads = "never"
python-preference = "only-system"
