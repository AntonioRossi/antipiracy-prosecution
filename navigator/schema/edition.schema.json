{
  "schemaVersion": "1",
  "type": "object",
  "required": [
    "editionVersion",
    "editionId",
    "displayName",
    "strategyName",
    "strategyPrefix",
    "consumerId",
    "claimPackageId",
    "claimSetVersion",
    "relationPath",
    "editionWordingPath",
    "artifactName",
    "declaredReleaseTimestamp",
    "independentClaims",
    "census",
    "groups"
  ],
  "additionalProperties": false,
  "properties": {
    "editionVersion": {"type": "string", "enum": ["1"]},
    "editionId": {"type": "string", "enum": ["na", "af"]},
    "displayName": {"type": "string", "minLength": 1},
    "strategyName": {"type": "string", "minLength": 1},
    "strategyPrefix": {"type": "string", "enum": ["NA", "AF"]},
    "consumerId": {"type": "string", "pattern": "^navigator-(na|af)$"},
    "claimPackageId": {"type": "string", "pattern": "^aa11393us-(na|af)-us-claim-set$"},
    "claimSetVersion": {"type": "string", "pattern": "^(NA|AF)-[0-9]{4}-[0-9]{2}-[0-9]{2}-v[1-9][0-9]*$"},
    "relationPath": {"type": "string", "pattern": "^navigator/relations/(na|af)__pct[.]relations[.]xml$"},
    "editionWordingPath": {"type": "string", "pattern": "^navigator/wording/(na|af)[.]wording[.]xml$"},
    "artifactName": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*[.]html$"},
    "declaredReleaseTimestamp": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"},
    "independentClaims": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "integer", "minimum": 1}
    },
    "census": {
      "type": "object",
      "required": ["claims", "units"],
      "additionalProperties": false,
      "properties": {
        "claims": {"type": "integer", "minimum": 1},
        "units": {"type": "integer", "minimum": 1}
      }
    },
    "groups": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    }
  }
}
