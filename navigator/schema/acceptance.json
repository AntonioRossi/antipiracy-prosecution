{
 "acceptanceVersion": "1",
 "comment": "Criteria and executable receipt-phase registry (TDD §14, guardrail 18). §14 must match the criterion text here (comparison test); the traceability matrix below maps each criterion to named live tests or QA-record fields, and the bidirectional meta-test closes both directions over this registry, itself included. The independently locked manualQaEvidenceVersion makes changes to nested manual-QA field grammar visible to the executable floor. receiptPhases is the exact closed phase/pass set bound into acceptance receipts. Applicability: a standalone edition release requires AC-01–AC-19; bundle creation additionally requires AC-20.",
 "receiptPhases": {
  "release-preflight": ["AC-01", "AC-02", "AC-03", "AC-04", "AC-05", "AC-06", "AC-07", "AC-08", "AC-09", "AC-10", "AC-11", "AC-12", "AC-13", "AC-14", "AC-15", "AC-16", "AC-17", "AC-18", "AC-19"],
  "release-postcondition": ["AC-16"],
  "bundle-postcondition": ["AC-20"]
 },
 "runner": {
  "runnerVersion": "1",
  "manualQaEvidenceVersion": "3",
  "editions": ["af", "na"],
  "testModules": [
   {"module": "test_acceptance", "path": "navigator/tests/test_acceptance.py"},
   {"module": "test_canon", "path": "navigator/tests/test_canon.py"}
  ],
  "fixtures": [
   {"path": "navigator/tests/fixtures/af_excerpt.json", "editions": ["af"]},
   {"path": "navigator/tests/fixtures/dispositions_fixture.json", "editions": []},
   {"path": "navigator/tests/fixtures/edition_parameters_af.json", "editions": ["af"]},
   {"path": "navigator/tests/fixtures/edition_parameters_na.json", "editions": ["na"]},
   {"path": "navigator/tests/fixtures/escaping_fixture.json", "editions": []},
   {"path": "navigator/tests/fixtures/golden_bundle.json", "editions": []},
   {"path": "navigator/tests/fixtures/migration_na_snapshot.json", "editions": ["na"]},
   {"path": "navigator/tests/fixtures/na_excerpt.json", "editions": ["na"]}
  ],
  "testScopes": [
   {"test": "test_acceptance.Acceptance.test_ac07_migration_scenarios", "editions": ["na"]}
  ],
  "supportFiles": [
   "AA11393US-claims-navigator_technical-description_DRAFT.md",
   "navigator/tests/__init__.py",
   "navigator/tests/vectors/canon_vectors.json",
   "navigator/tools/make_fixtures.py",
   "navigator/tools/stamp.py",
   "navigator/tools/sync_tdd_examples.py"
  ]
 },
 "criteria": [
  {"id": "AC-01", "applicability": "edition",
   "text": "Source extraction reproduces the edition's locked normative §3 parameter table and census exactly (corpus IDs/versions, group names/counts, display prefix, relation, gate, dependency, and config paths, QA bindings, forbidden terms, and artifact default), including the authored dependency map cross-validated against parsed claim references; PCT claims 1–18, Examples 1–5, Tables 1–3 with row anchors, four figures present and hash-verified.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac01_census_extraction"], "qaRecordFields": []}},
  {"id": "AC-02", "applicability": "edition",
   "text": "Every owner carries its applicable lifecycle fields with a valid review.contentHash over the review projection (incl. identity tuples and dependency-chain hash), plus an identified authorized reviewer with explicit operatorKind: human | model and a real canonical YYYY-MM-DD review date; tool is never release-authoritative; zero pending, zero stale.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac02_lifecycle"], "qaRecordFields": []}},
  {"id": "AC-03", "applicability": "edition",
   "text": "Gate referential integrity passes in both directions with declared uniqueness keys; every mandatory gate has a reviewed disposition, and every affirmative disposition, including one for an optional gate, has its actual matching carried evidence; the no-candidate disposition fixture releases without a fabricated mapping; the inventory-completeness attestation by an identified authorized human/model operator is current, and tool evidence is rejected.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac03_gate_integrity"], "qaRecordFields": []}},
  {"id": "AC-04", "applicability": "edition",
   "text": "Zero unresolved source drift: all corpus, fragment, aggregate-claim, dependency-chain, target, and caution-source digests current (full digests, matching canonVersion).",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac04_source_drift"], "qaRecordFields": []}},
  {"id": "AC-05", "applicability": "edition",
   "text": "All schema and invariant validations pass: axis tags present with inter-axis invariants and declared locator exceptions satisfied; applicability matrices; relation-binding conformity; phrase rules; no duplicate targets, gate instances, or dispositions; note and rationale bounds; caution type×scope matrix and code closure; caution sources resolve into quotable blocks only; zero targets on editorial, excluded, or internal blocks; the support matrix has its closed structural shape; enum ↔ presentation coverage; forbidden-terms check clean; the centralized authority matrix admits only explicit human/model kinds paired with an NFC identity having no surrounding whitespace or control/format characters and at least one visible character, and rejects tool, missing, blank, invisible-only, unknown, and malformed kinds or identities.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac05_authorized_operator_matrix", "test_acceptance.Acceptance.test_ac05_schema_invariants"], "qaRecordFields": []}},
  {"id": "AC-06", "applicability": "edition",
   "text": "The edition's fixtures (including the disposition fixture) validate against schemas and pinned corpora; this document's examples match the fixture projections.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac06_fixtures"], "qaRecordFields": []}},
  {"id": "AC-07", "applicability": "edition",
   "text": "Registry-access, artifact-kind/path, edition-blindness, writer-set, diff-classifier, single-digest-path, single-JSON-input-parser, and procedure-vs-matrix tests pass; the NA-scoped migration case-table scenario test passes against a locked synthetic NA snapshot rather than live NA inputs, asserts the runtime classifier accepts every intended delta and rejects semantic/review mutations before any source write, while other edition releases neither execute nor bind it; the registered canonical serialization golden-vector and generated-input property tests pass (idempotence, NFC duplicate-key rejection, escaping exactness, and integer edge rules).",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac07_meta_tests", "test_acceptance.Acceptance.test_ac07_migration_scenarios", "test_canon.TestProperties.test_code_idempotent", "test_canon.TestProperties.test_digest_list_order_sensitivity", "test_canon.TestProperties.test_domain_separation", "test_canon.TestProperties.test_integer_edges", "test_canon.TestProperties.test_json_escaping_exactness", "test_canon.TestProperties.test_json_key_sorting_codepoint", "test_canon.TestProperties.test_prose_idempotent", "test_canon.TestVectors.test_golden_vectors", "test_canon.TestVectors.test_json_parser_rejects_information_losing_inputs", "test_canon.TestVectors.test_known_sha256", "test_canon.TestVectors.test_law_edges", "test_canon.TestVectors.test_tag_registry", "test_canon.TestVectors.test_unicode_pin"], "qaRecordFields": []}},
  {"id": "AC-08", "applicability": "edition",
   "text": "Projection tests pass: no never-tagged field and no internal QA-source identifier appears in any artifact kind, previews included.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac08_projections"], "qaRecordFields": []}},
  {"id": "AC-09", "applicability": "edition",
   "text": "Adversarial escaping fixtures render safely through every authored and quoted channel.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac09_escaping"], "qaRecordFields": []}},
  {"id": "AC-10", "applicability": "edition",
   "text": "Forward and reverse navigation behave per §§5–6, including claim-level gate display (scripted checks on the built candidate).",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac10_navigation"], "qaRecordFields": []}},
  {"id": "AC-11", "applicability": "edition",
   "text": "Keyboard-only operation, focus determinism, browser-native computed accessible names/roles/states, and polite live-region updates per §12 are checked against candidate bytes for every atomic release-approved support-matrix row. The versioned QA evidence copies the exact ordered target set, records actual browser/OS/AT versions and the configured traversal chord, and carries typed passed results for traversal, activation keys, focus, scoped arrows, computed semantics, and live-region mutation. Rows with at: \"none\", including Safari, require browser-native evidence but no external assistive technology; a named-AT row requires that exact named technology and version. VoiceOver is outside this delivery contract. Results are recorded by the same identified authorized human/model operator; tool is never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac11_accessibility_static"], "qaRecordFields": ["ac11"]}},
  {"id": "AC-12", "applicability": "edition",
   "text": "Print output per §12 is checked against candidate bytes and recorded as versioned structured QA evidence with the actual browser/OS versions, positive page count, and typed passed results for readable claims/disclosure, the disclaimer on every printed page, the legend on every printed page, provenance, schedule, and absence of content clipping, by the same identified authorized human/model operator; tool is never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac12_print_static"], "qaRecordFields": ["ac12"]}},
  {"id": "AC-13", "applicability": "edition",
   "text": "Local-file operation across every atomic row of schema/support-matrix.json is checked against the exact candidate bytes. Versioned structured QA evidence copies the exact ordered target set, records actual browser/OS/AT versions, the exact minimum and a below-minimum viewport, and typed passed local-file, minimum-layout, and stacked-layout results, by the same identified authorized human/model operator; tool is never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac13_layout_static"], "qaRecordFields": ["ac13"]}},
  {"id": "AC-14", "applicability": "edition",
   "text": "With JavaScript disabled, the §11 static content is fully readable on the candidate authorized by an identified human/model QA operator; tool is never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac14_nojs_static"], "qaRecordFields": []}},
  {"id": "AC-15", "applicability": "edition",
   "text": "Self-auditing attempted-use instrumentation per schema/api-policy.json exposes the exact registered probe set and fails closed unless every hook reports installed with null error/detail. Versioned structured candidate-bound QA evidence records the actual browser/OS versions, ready=true, the exact installed hook map, and exact empty attempted-use, resource, external-request, cookie-write, Web Storage, IndexedDB, and navigation-mutation ledgers; it is authorized by an identified human/model operator, never tool.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac15_api_policy"], "qaRecordFields": ["ac15"]}},
  {"id": "AC-16", "applicability": "edition",
   "text": "Double build byte-identical across separate interpreter processes; content-input lock reproduces byte-identically between candidate and release (diagnostics excluded); exact-set check passes in both directions; release byte-compares against the QA'd candidate and seals the same bytes; lock, envelope, and release-record written append-only; checksums emitted and matching. The candidate-bound qaInputLock over pin-verified internal QA inputs also reproduces before release. The release record is appended last only after sealed/checksum readback, is authorized by an identified human/model operator, and carries a current registry/runner/subject-bound acceptance receipt whose exact release-preflight result covers AC-01–AC-19 and whose post-write release-postcondition covers AC-16; the tool runner supplies evidence but is never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac16_determinism"], "qaRecordFields": []}},
  {"id": "AC-17", "applicability": "edition",
   "text": "§9.1 disclaimer and approved legend present on screen, in print, and no-JS; legend approval by an identified authorized human/model operator matches the shipped wording digest, and tool evidence is rejected.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac17_disclaimer_legend"], "qaRecordFields": []}},
  {"id": "AC-18", "applicability": "edition",
   "text": "Double-sided QA attestations by identified authorized human/model operators are current and carry producerCommand: navigator/build.py attest/v1, the exact typed attest-command output contract: priority-map cross-check and, for AF, the crosswalk non-conflation check; missing or unknown producer metadata and tool evidence are rejected. The producer marker is digest-bound audit metadata under the repository/Git trust root, not a cryptographic signature.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac18_attestations"], "qaRecordFields": []}},
  {"id": "AC-19", "applicability": "shared",
   "text": "Before importing any registry-selected callback, the production runner rejects drift from an independent locked executable floor: the supported-edition set, module path specifications, fixture path→scope map, callback test scopes, support-file inventory, manual-QA-evidence version, and exact per-criterion callback/QA-field map are deletion- and redirection-resistant, with unique callback ownership. The traceability meta-test then proves every registered criterion (AC-01 through AC-20, including this one) maps to its named live tests with criterion-matching test_acNN_ identity or QA-record fields, only AC-07 owns canonicalization callbacks, every acceptance-designated test maps back to a registered criterion, and every floored support input and every shared or active-edition fixture exists and is locked without opening inactive-edition fixture bytes.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac19_traceability_meta"], "qaRecordFields": []}},
  {"id": "AC-20", "applicability": "bundle",
   "text": "The bundle matches its config exactly (enumerated members only, including the bundle-manifest), every member digest verified, deterministic STORE ZIP conforming to the golden bundle fixture with its own detached checksum, and the neutral manifest matching its approved wording digest. Every configured release-record resolves by its explicit digest to a complete passed release→QA→exact-attestation chain with exact typed producer metadata and authorization by identified human/model operators, matches independently derived current candidate/content-lock bindings, exact current qaInputLock (including the raw selected-QA-registry read), current support-matrix approver, exact live ordered support targets/viewport, current API probe set, and the locked manual-QA evidence version; bundle verification independently revalidates the typed AC-11/12/13/15 evidence rather than trusting QA-copied context. The manifest approval is current identified authorized human/model evidence. Every resolved release receipt is current, and the bundle record is appended last only after manifest/ZIP/checksum readback by an identified authorized human/model operator with a current registry/runner/subject-bound bundle-postcondition receipt covering AC-20; tool records and the tool runner are never authority.",
   "enforcedBy": {"tests": ["test_acceptance.Acceptance.test_ac20_bundle"], "qaRecordFields": []}}
 ]
}
